spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KC_SERVER_URL:http://localhost:9000}/realms/${KC_REALM:museum}
          jwk-set-uri: ${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

keycloak:
  properties:
    serverUrl: ${KC_SERVER_URL:http://localhost:9000}
    realm: ${KC_REALM:master}
    username: ${KC_USERNAME:admin}
    password: ${KC_PASSWORD:admin}
    client-id: ${KC_CLIENT_ID:museum-api}
    client-secret: ${KC_CLIENT_SECRET:AaBvy4eAYlDjqzKFPOCxQRvFptKrqael}
    jwt:
      preferred-name: sub